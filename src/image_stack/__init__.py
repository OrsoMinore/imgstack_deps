"""image_stack: curated dependency bundle for image processing projects.

Installing this package pulls in a vetted set of the most commonly needed
image-processing libraries (numpy, pillow, scikit-image, opencv, etc.) so
new projects can start with a single dependency instead of hand-picking one
by one.
"""

__version__ = "0.2.1"

__all__ = ["__version__"]
